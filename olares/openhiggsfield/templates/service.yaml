apiVersion: v1
kind: Service
metadata:
  name: openhiggsfield
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: openhiggsfield
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
